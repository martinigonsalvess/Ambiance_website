import * as bootstrap from "bootstrap";

const toTop = document.querySelector(".pijl-boven");
window.addEventListener("scroll", () => {
  if (window.pageYOffset > 100) {
    toTop.classList.add("active");
  } else {
    toTop.classList.remove("active");
  }
});
